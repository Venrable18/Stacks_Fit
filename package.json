{
  "name": "stacksfit-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd Frontend && npm ci && npm run build",
    "dev": "cd Frontend && npm run dev"
  },
  "workspaces": [
    "Frontend",
    "ai_middleware"
  ]
}